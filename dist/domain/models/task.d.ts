export declare class Task {
    id: number;
    title: string;
    description?: string;
    completed?: boolean;
    constructor(id: number, title: string, description?: string, completed?: boolean);
}
